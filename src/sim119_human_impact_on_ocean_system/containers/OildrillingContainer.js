import { connect } from "react-redux";
import Oildrilling from '../components/TopSection/Oildrilling';

const mapState = (state) => ({});
const mapDispatch = (dispatch) => ({});

export default connect(mapState, mapDispatch)(Oildrilling);
