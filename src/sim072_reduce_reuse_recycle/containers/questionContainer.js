import { connect } from "react-redux";
import QuestionSection from "../components/questionView";

const mapState = (state) => ({})

const matchDispatch = (dispatch) => ({
});

export default connect(mapState, matchDispatch)(QuestionSection);